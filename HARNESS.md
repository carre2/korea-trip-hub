# HARNESS — Korea Trip Hub 사실검증 하네스

> 이 문서는 **여행정보 사이트에 하네스 엔지니어링**을 적용한 제어 장치다.
> 목적은 단 하나: **LLM(또는 사람)이 여행 사실을 지어내지 못하게 막는 것**.
> 여행정보는 비자·요금·전화번호·시간표·주소처럼 **틀리면 실제 여행자에게 피해가 가는 사실**을 다룬다.
> 핵심 원리: 모델의 자유 생성을 **고정된 사실대장(facts SSOT)** 과 **출처·검증일 강제**로 감싼다.
>
> 소설 하네스의 `canon`에 해당하는 것이 여기서는 `data/facts.json`이다. **facts가 왕이다.**

---

## 0. 절대 규칙 (THE LAW)

1. **facts.json에 없는 사실 수치는 본문에 못 쓴다.**
   가격·전화번호·주소·소요시간·거리·시간표·통계·비자규정·환율은 전부 "사실(fact)"이다.
2. **모르면 지어내지 않는다.** 대신:
   - `data/facts.json`에 `"status": "TODO"` 항목으로 남기거나
   - 본문에 `{{FACT:id}}` 플레이스홀더를 쓰고 검증 전엔 렌더링하지 않는다.
   - **그럴듯한 숫자를 채워 넣는 것은 금지.** (예: "약 45,000원", "보통 20분" 같은 임의 추정 금지)
3. **모든 fact에는 `source`(공식 URL 우선)와 `verified`(검증일)가 있어야 한다.** 없으면 미검증.
4. **변동정보(volatile)는 항상 공식 출처 링크와 "as of DATE" 라벨을 함께 노출한다.**
   (비자/K-ETA·요금·환율·콘서트 일정·운영시간은 volatile)
5. **facts와 본문이 다르면 멈추고 사람에게 확인.** 임의로 한쪽을 바꾸지 않는다.

> 판단 기준: "이 문장이 틀리면 여행자가 손해 보는가?" → 예이면 그것은 fact이고, facts.json 근거가 있어야 한다.

---

## 1. 사실 등급 (fact tiers)

| 등급 | 예시 | 규칙 |
|------|------|------|
| **STABLE** | 긴급번호(112·119), 1330, 지리·문화 상식 | 출처+검증일 필요. 재검증 주기 길다(연 1회) |
| **VOLATILE** | 비자·K-ETA, 교통요금, 환율, 운영시간, 콘서트 일정 | **공식 출처 링크 + "as of DATE" 필수 노출.** 재검증 주기 짧다 |
| **EDITORIAL** | "명동은 쇼핑으로 유명하다" 같은 정성적 서술 | 수치가 없으면 fact 아님. 단, 통계 주장하면 fact로 승격 |
| **TODO** | 아직 미검증 | 렌더링 금지. 목록에만 존재 |

---

## 2. facts.json 스키마 (SSOT)

```json
{
  "id": "keta-status",
  "tier": "VOLATILE",
  "claim": "비자면제국 국민은 2026-12-31까지 K-ETA 없이 입국 가능(e-Arrival Card는 필수).",
  "value": { "exempt_until": "2026-12-31", "earrival_card": "required" },
  "source": "https://english.visitkorea.or.kr/...",
  "source_name": "VisitKorea (공식)",
  "verified": "2026-08-02",
  "recheck_after": "2026-10-01",
  "status": "VERIFIED",
  "notes": "출처별 면제 국가 수 상이(22/67). 반드시 공식에서 최종 확인 라벨 노출."
}
```

- `status`: `VERIFIED` | `TODO` | `STALE`(검증일이 recheck_after 지남) | `DISPUTED`(출처 충돌)
- 본문/컴포넌트는 **`status !== "VERIFIED"` 인 fact를 렌더링하지 않는다** (또는 "확인 중" 표시).

---

## 3. 콘텐츠 작성 루프 (카테고리 글마다 반복)

```
[1] 로드   → HARNESS.md(이 문서) + data/facts.json + 해당 카테고리 데이터
[2] 초안   → EDITORIAL 서술은 자유롭게. 단 수치가 필요한 자리엔 {{FACT:id}} 만 넣는다
[3] 검증   → 각 {{FACT:id}}에 대해:
             - facts.json에 VERIFIED로 있으면 통과
             - 없으면 웹으로 공식 출처 확인 → facts.json에 추가(source+verified)
             - 확인 불가면 TODO로 남기고 그 문장은 뺀다 (지어내지 않음)
[4] 체크   → §4 체크리스트 통과
[5] 커밋   → facts: / content: 접두어로 작게 커밋
```

> 가장 중요한 단계는 **[3] 검증**. 여기서 "확인 불가"를 "그럴듯한 값"으로 메우는 순간 하네스는 무너진다.

---

## 4. 검증 체크리스트 (글마다 통과)

**사실성 (facts 대조)**
- [ ] 본문의 모든 숫자(가격·시간·거리·번호·%)가 facts.json의 VERIFIED 항목에 근거하는가
- [ ] 임의 추정치("약", "보통", "대략")로 수치를 지어내지 않았는가
- [ ] VOLATILE 항목에 공식 출처 링크 + "as of DATE"가 함께 노출되는가
- [ ] 전화번호·주소·URL을 실제로 출처에서 확인했는가 (기억으로 쓰지 않았는가)

**안전 (여행자 보호)**
- [ ] 비자·입국·의료·긴급 정보에 "공식에서 최종 확인" 안내가 있는가
- [ ] 오래된 정보(STALE)를 최신처럼 보이게 하지 않았는가

**다국어**
- [ ] 번역이 사실(숫자·고유명사)을 바꾸지 않았는가 (facts는 언어 무관 단일 소스)

**출처**
- [ ] 새로 쓴 fact는 facts.json에 source+verified와 함께 추가됐는가 (없으면 "facts 갱신 없음" 명시)

---

## 5. 금지 목록 (하드 룰)

- ❌ 가격/요금을 기억이나 추정으로 쓰기 (반드시 출처)
- ❌ 전화번호·주소를 확인 없이 쓰기
- ❌ "보통 20분", "약 5만원" 같은 임의 근사치로 수치 자리를 메우기
- ❌ 콘서트/공연 일정을 임의 생성 (→ 반드시 kpophub 데이터/공식 티켓)
- ❌ 통계("방문자 60%가…")를 출처 없이 인용
- ❌ 검증 안 된 fact를 VERIFIED로 표기
- ✅ 대신: TODO로 남기고 사람에게 "이 값 확인 필요"라고 보고

---

## 6. 운영

- **facts가 왕이다.** 충돌 시 멈추고 확인.
- 한 번에 한 카테고리씩. 대량 일괄 생성은 검증 누락을 키운다.
- 매 작업 끝 **facts 갱신 여부 명시**(없으면 "갱신 없음").
- 컨테이너/로컬은 사라질 수 있으니 **중간중간 GitHub push**.
- 커밋 접두어: `facts:` / `content:` / `feat:` / `i18n:`.

## 7. 빠른 시작

1. 이 문서와 `data/facts.json`을 먼저 읽는다.
2. 작성할 카테고리를 고른다.
3. §3 루프로 초안 → §4 체크리스트 → 커밋.
